sudo: required

language: ruby

services:
  - docker

before_install:
  - docker build -t core23/jenkins-php-ci .
  - docker run -d --name jenkins-php -p 8080:8080 core23/jenkins-php-ci
  - docker ps -a
